let birthYear = 1998;
let futureYear = 2040;

let possibleAge = futureYear - birthYear;

let outPut = 'I will either be '+ possibleAge + ' or ' + ++possibleAge + ' in ' + futureYear + '.';

console.log(outPut);